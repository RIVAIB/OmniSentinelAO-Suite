# Copy to .env.docker and fill in values
# Run: docker-compose --env-file .env.docker up -d

# Required
OPENAI_API_KEY=sk-...

# Optional — change if defaults conflict with your system
POSTGRES_PASSWORD=mem0pass
NEO4J_PASSWORD=mem0graph
